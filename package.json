{
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "prettier": "3.2.4",
    "turbo": "^1.11.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*"
  ]
}
